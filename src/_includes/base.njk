<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {%- set documentTitle %}{% if title %}{{ title }} — {% endif %}{{ metadata.siteTitle }}{% endset %}
  <title>{{ documentTitle }}</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<div class="wrapper">

<header>
  <p><a href="{{ '/' | url }}">{{ metadata.siteTitle }}</a></p>
</header>

<main>
{{ content | safe }}
</main>

</div><!-- /.wrapper -->
</body>
</html>
